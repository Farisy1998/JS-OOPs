// Traspiler -> Translator + Compiler
// Babel is one of famous transpiler to compile and translate our JS code
// to the format a browser can understand
//
//   modern JS -----> BABEL -------> ES5

// Bundler -> Tool used to bundle our .js modules to a single file (ie, bundle.js)
// A famous bundler is WebPack. Which bindles our .js modules or files into a
// single .js file by removing the commends, white spaces, shorten the name of
// our identifiers like classes, functions, variable etc. And this will reduce
// the size of bundle file that we served to the client.